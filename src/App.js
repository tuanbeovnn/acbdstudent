import React, {Component} from 'react';
import { library } from '@fortawesome/fontawesome-svg-core';
import { faChalkboardTeacher, faCalendarWeek, faCommentDots, faCreditCard, faInfoCircle, faCheckCircle } from '@fortawesome/free-solid-svg-icons';

library.add(faChalkboardTeacher, faCalendarWeek, faCommentDots, faCreditCard, faInfoCircle, faCheckCircle);

class App extends Component{
    render() {
        return (
            <div></div>
        );
    }
}

export default App;